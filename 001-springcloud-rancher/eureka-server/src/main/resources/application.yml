server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server
  client:
    register-with-eureka: false #禁用 Eureka Server 自注册
    fetch-registry: false #禁用从 Eureka Server 获取注册信息
spring:
  application:
    name: cloud-eureka-server